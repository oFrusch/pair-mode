export {
  applyMouse,
  cursorRowIndex,
  moveSelectionHead,
  normalizeSelection,
  selectionSpanFor,
  startSelection,
  wholeRowSelection,
} from "./selection";
export type { Selection } from "./selection.types";
