export { loadConfig, saveConfig, configPath, DEFAULT_CONFIG } from "./config";
export type {
  PairConfig,
  EditorName,
  MultiplexerName,
  TransportName,
  Layout,
  Theme,
  Pane,
  Session,
  Web,
  NotePosition,
} from "./types";
