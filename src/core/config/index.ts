export { loadConfig, saveConfig, configPath, DEFAULT_CONFIG } from "./config";
export { paintLayout } from "./layout";
export type {
  PairConfig,
  EditorName,
  MultiplexerName,
  TransportName,
  Layout,
  PaintLayout,
  Theme,
  Pane,
  Session,
  Web,
  NotePosition,
  ConfigResult,
} from "./types";
